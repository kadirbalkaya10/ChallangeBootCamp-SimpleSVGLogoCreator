class Shapes {
  constructor(logoText, logoTextColor, shape, shapeColor) {
    this.logoText = logoText;
    this.logoTextColor = logoTextColor;
    this.shape = shape;
    this.shapeColor = shapeColor;
  }
}

module.exports = Shapes;
